#!/usr/bin/python3
"""Define a matrix multiplication function"""


def matrix_mul(m_a, m_b):
    """Multiply two matrices

    Args:
        m_a (list of lists of int/floats): The first matrix.
        m_b (lists of lists of instance floats) the second matrix.

    Raises:
        TypeError: if either m_a or m_b is empty, or if they are not lists.
        TypeError: if either m_a or m_b has different sized rows.
        ValueError: if m_a and m_b cannot be multiplied.

    Returns:
        A new matrix representing the multiplication of m_a by m_b
    """

    if not`:wq


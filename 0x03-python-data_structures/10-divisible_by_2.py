#!/usr/bin/python3
def divisible_by_2(my_list=[]):
    result = []
    for i, num in enumerate(my_list):
        if num % == 0:
            result.append(True)
            print(f"{y} is divisble by 2")
        else:
            result.append(False)
            print(f"{y} is not divisible by 2")
    return result
